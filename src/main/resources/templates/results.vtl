<h1>All Pets!</h1>
#if($pets)
  <h3>
    <ul>
    #foreach ($pet in $pets)
      <li>Name: $pet.getName()</li>
      <li>Food level: $pet.setFoodLevel(0)</li>
      <li> Is Alive?: </li>
      <li>$pet.isAlive()</li>
      #end
    </ul>
#else
    <p>
      No Pet Exists!
    </p>
  </h3>
#end
<h3><a href="/">Go make another pet!</a></h3>
